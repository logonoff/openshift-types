{
  "name": "openshift-types",
  "packageManager": "yarn@4.5.2",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@types/node": "^24.3.1",
    "json-schema-to-typescript": "^15.0.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "yaml": "^2.8.1"
  },
  "scripts": {
    "generate:collect": "./collect-crds.sh",
    "generate:types": "mkdir -p ./generated/types && cp ./src/K8sResourceCommon.d.ts ./generated/types/ && ts-node src/generate-types.ts",
    "generate": "yarn clean && yarn generate:collect && yarn generate:types",
    "clean": "rm -rf ./generated"
  }
}
